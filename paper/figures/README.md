# Figures

All six figures of the paper (architecture, sequence diagram,
algorithm, and the three results plots) are implemented as native
TikZ/pgfplots code embedded directly in `../main.tex`, so they are
compiled vectorially with the manuscript. No external image files are
required. To reproduce the results plots from raw data, see
`../../notebooks/reproduce_figures.ipynb`.
